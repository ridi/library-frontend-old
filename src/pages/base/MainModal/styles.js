import { modal } from '../../../styles';

export const filterModal = { ...modal };

export const sortModal = { ...modal };

export const modalActive = {
  opacity: 1,
  transform: 'translate3d(0, 0, 0)',
  pointerEvents: 'initial',
};
