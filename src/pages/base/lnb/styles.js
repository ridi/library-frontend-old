import { css } from 'emotion';

export const LNBTabBarWrapper = css({
  height: 40,
  backgroundColor: '#ffffff',
  borderBottom: '1px solid #d1d5d9',
});

export const LNBTitleBar = css({});
export const LNBTitleBarBackButton = css({});
