import { css } from '@emotion/core';

export const responsive = css({
  position: 'relative',
  width: '100%',
  maxWidth: 1000,
  margin: '0 auto',
  padding: '0 16px',
  boxSizing: 'border-box',
  overflow: 'hidden',
});
